query cameraData {
  cameraData {
    database {
      id
      key
      name
      path
      protocol
      resolution
      enableCamera
      customPipeline
      framesPrSecond
      bitratePrSecond
      contrast
      rotation
      brightness
      whiteBalance
      flipCamera
    }
    availableCams {
      key
      value
      text
      caps {
        value
        text
        height
        width
        format
      }
    }
  }
}
