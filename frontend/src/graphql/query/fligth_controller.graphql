query flightController {
  flightController {
    data {
      id
      controller
      protocol
      connectionType
      internalAddress
      baudRate
      tcpPort
      binFlightLog
    }
  }
}
