query getEndpoints {
  getEndpoints {
      data {
        id
        telemEnable
        moduleActive
        name
        endpointIPaddress
        telemetryPort
        videoPort
        videoEnable
      }
  }
}
