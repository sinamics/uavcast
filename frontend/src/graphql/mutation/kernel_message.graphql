mutation childProcessCmd($cmd: String!, $path: String!, $shell: Boolean, $logg: Boolean) {
  childProcessCmd(cmd: $cmd, path: $path, shell: $shell, logg: $logg) {
    message
    errors {
      path
      message
    }
  }
}
